# AGENTS.md

本文件是 ExpertBrain 项目的 Agent 总规则。Codex、Cursor Agent、Claude Code、Copilot 或其他代码 Agent 在本仓库工作时必须优先阅读本文件。

## 1. 项目宗旨

ExpertBrain 的核心宗旨是：**构建可自定义知识库的检索增强问答系统，让用户能够围绕自己的私有/行业知识库进行可靠的问题检索、依据召回和回答生成。**

项目所有功能、架构和优化都应围绕以下主线展开：

- 知识采集：支持将法规、政策、平台规则、企业文档、客户资料等知识资产导入系统。
- 知识治理：支持知识库、文档、标签、来源、版本、状态、生命周期管理。
- 文档处理：支持解析、清洗、切片、向量化、重建索引和质量诊断。
- 检索增强：支持关键词检索、向量检索、混合检索、排序、引用溯源。
- 问答生成：基于检索结果生成有依据、可追溯、可解释的回答。
- 业务适配：围绕特定行业/业务场景沉淀专用知识库和问答能力。

任何新需求都必须优先判断是否服务于“自定义知识库问题检索回答”这个核心目标。

如果需求明显偏离该宗旨，例如变成通用爬虫平台、通用办公系统、CRM、ERP、自动化运营平台、复杂多 Agent 工作流、模型训练平台或无关工具集合，Agent 必须主动提醒用户：该需求可能偏离 ExpertBrain 主线，并建议将其降级为外部工具、数据源预处理脚本或后续独立项目。

## 2. 项目目标

ExpertBrain 是企业行业专家系统，目标是帮助企业快速构建面向特定业务领域的专家知识库，并通过 RAG 能力完成问题检索、依据召回、回答生成和引用追溯。

V1 首期场景是山东省政府采购业务，包括：

- 政府采购法规、政策、通知、流程问答
- 京东慧采、青慧采、泉E采等平台知识管理
- 供应商经营范围、主营产品、目标平台、资质情况分析
- 平台推荐、服务流程推荐、发品建议、风险提示、方案生成

这些业务功能必须服务于知识库问答主线；凡是不能增强知识采集、治理、检索、问答或引用追溯能力的需求，默认不进入 V1。

## 3. 技术栈约束

除非用户明确要求，否则不要随意替换技术栈。

- Backend: Laravel 11+ API
- Frontend: Vue 3 + Vite + Element Plus + Tailwind CSS
- Database: PostgreSQL 16+ + pgvector
- AI Service: Python 3.11+ + FastAPI
- Queue: Redis + Laravel Queue
- Search: PostgreSQL full-text search + pgvector hybrid retrieval
- Embedding: BGE-M3
- LLM: API 优先，本地模型作为可插拔实现
- Deployment: Docker Compose + Traefik

## 4. 架构原则

1. Laravel 负责业务系统、权限、客户、知识库、规则、方案等核心业务。
2. FastAPI 负责文档解析、chunk、embedding、RAG 检索、Prompt 编排、LLM 调用。
3. PostgreSQL 是主数据库，pgvector 直接承载向量，不引入额外向量库作为 V1 依赖。
4. 确定性业务逻辑必须优先使用规则引擎，不允许完全交给 LLM 判断。
5. LLM 输出必须尽量带引用。没有依据时必须返回“不确定”或“知识库中暂无依据”。
6. 网络站点采集、PDF 下载、网页抓取等能力应优先作为知识库数据源预处理流程，不应把 ExpertBrain 做成通用爬虫平台。
7. 页面、接口、任务和数据结构的优化，应优先提升“导入是否可靠、切片是否合理、检索是否准确、回答是否可追溯”。

## 5. V1 禁止事项

V1 不做以下事情：

- 不自动登录或操作京东慧采、青慧采、泉E采等第三方平台。
- 不自动提交资质、发布商品、修改客户平台数据。
- 不做模型微调。
- 不做复杂多 Agent 自动工作流。
- 不把法规结论写死在 Prompt 中。
- 不让 LLM 在没有检索依据的情况下输出肯定结论。
- 不把项目扩展成通用爬虫系统、通用 CRM、通用 ERP、通用自动化办公平台。

## 6. 开发顺序

严格按以下顺序推进：

1. 项目脚手架：Laravel、Vue、FastAPI、Docker Compose。
2. 认证与 RBAC。
3. 知识库基础表、分类、标签、文档管理。
4. 文档解析与入库。
5. chunk 与 embedding。
6. hybrid search。
7. RAG 问答与引用返回。
8. 客户画像与平台规则。
9. 服务方案生成。
10. 案例库、统计、知识生命周期。

## 7. 需求判断规则

每个新增需求先归类：

- A 类：直接增强知识库问答能力。优先做。
- B 类：作为知识库数据源或预处理工具。可以做，但应与主系统解耦。
- C 类：业务辅助功能，但与检索问答弱相关。进入 backlog，不默认进入 V1。
- D 类：偏离项目宗旨。必须提醒用户需求跑偏，建议作为独立项目或暂缓。

判断标准：

1. 是否能提升知识导入、解析、切片、向量化质量？
2. 是否能提升检索准确率、召回质量、排序质量？
3. 是否能提升回答可靠性、引用溯源、可解释性？
4. 是否能帮助用户围绕自定义知识库提出问题并得到可靠回答？
5. 是否只是泛化业务系统能力而没有服务 RAG 主线？

如果第 1-4 项都不满足，通常应判定为偏离主线。

## 8. 提交规范

提交信息使用 Conventional Commits：

- feat: 新功能
- fix: 修复
- docs: 文档
- refactor: 重构
- test: 测试
- chore: 工程配置

## 9. 每次开发前必须检查

- 是否阅读了 docs/02-PRD.md？
- 是否阅读了 docs/05-Database-Design.md？
- 是否阅读了 docs/06-API-Specification.md？
- 是否阅读了对应 .cursor/rules 文件？
- 是否破坏了“确定性规则优先于 LLM”的原则？
- 是否偏离了“自定义知识库问题检索回答”的项目宗旨？

## 10. 质量要求

- 后端接口必须有请求校验。
- 重要写操作必须记录审计日志。
- RAG 结果必须包含引用来源字段。
- 数据库 migration 必须可重复执行和回滚。
- API 响应结构必须一致。
- 前端页面必须有空状态、加载状态、错误状态。
- 知识导入、解析、切片、向量化任务必须可观察、可重试、可诊断。
